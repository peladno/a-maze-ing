# 2026-09-18 — so

| | |
| --- | --- |
| **Author / 担当** | so |
| **Time spent / 作業時間** | 0h00 |
| **Branch / ブランチ** | `main` |
| **Focus / 今日の主題** | Check javi's integration end to end, and agree on the last open points |

---

## 1. Done / やったこと

- **Ran the whole program for the first time** (late on 2026-09-17, after the merge). Before javi's entry point
  landed, a throwaway script (never committed) wired `load_config`, `MazeGenerator`, `shortest_path`, `MazeWriter`
  and `TerminalRenderer` together, to confirm the parts fit. They did: the analyzer judged the output
  Pac-Man-USABLE, and an entry on the "42" stopped with the `SolveError` message.
- **Checked javi's integration** — `commit e6a5988` to `commit 371b2d7`, in a separate worktree. flake8, mypy and
  129 tests pass; `python3 a_maze_ing.py config.txt` prints the seed, writes `maze.txt` and opens the menu. The
  "42" is now filled in magenta. Error cases — entry on the "42", missing config, a 2x2 board — print a message and
  exit 1 (§IV.2).
- **Found four things and sent them to javi:** `r` redrew the same maze instead of regenerating; the path, entry
  and exit were attached to the `Maze` with `setattr`; generated `maze*.txt` files were committed or untracked; and
  warnings on stderr can appear before stdout lines when output is piped.
- **Agreed with javi (Discord):** `r` draws a **new seed even when `SEED` is set** and rewrites the output file;
  sample configs go into a directory; javi will look at the `setattr`. Recorded in `01_kickoff.md` 4.3.

## 2. Next / 次にやること

1. Check javi's next update: regeneration, the renderer taking its data as arguments, and the §VI wheel.
2. Update `CLAUDE.md` §6, which still says the entry point is on an unmerged branch.
3. Check the README against the running program.

## 3. Stuck / 詰まったこと

| What / 事象 | What I tried / 試したこと | Resolved? / 解決 | Needs / 必要なもの |
| --- | --- | --- | --- |
| Was pressing `r` supposed to give the same maze when the seed is fixed? | Pressed `r` then `q` and compared the two drawings line by line: identical, and the seed printed once. `r` did nothing yet — the seed was not the reason. | yes | agreed a new seed with javi |
| Comparing two drawings failed on a line count (31 against 30). | The input prompt has no newline, so the next drawing's first line shared its line. | yes | — |

## 4. Learned / 学んだこと

- **A fixed seed and a "regenerate" key pull in opposite directions.** The seed is for reproducing one maze; the
  key is for seeing another. Saying which one each is for settled the design in one message.
- **Parts that pass their own tests can still fail to fit.** Running them together once, even from a throwaway
  script, showed the "42" was invisible — something no unit test on either side would catch.

## 5. For the pair / 相方への共有

- Thanks for the integration — the whole pipeline works, and the magenta "42" meets §IV.4.
- Still open on your side: `r`, the `setattr` on `Maze`, and ignoring generated output files.
